# Required for agent reply generation and LLM-as-judge evaluation
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini

# Optional: set to 1 to skip LLM calls (uses rule-based fallbacks only)
DEMO_MODE=0
